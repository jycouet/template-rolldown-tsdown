{
  "name": "template-rolldown-tsdown",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build:rolldown && npm run build:tsdown",
    "build:rolldown": "rolldown --config rolldown.config.js",
    "build:tsdown": "tsdown",
    "test": "npm run build && node test.js"
  },
  "devDependencies": {
    "rolldown": "1.0.0-beta.46",
    "tsdown": "^0.15.2"
  }
}
